<h1>My Team's Achievements</h1>
<hr/>

<ul class='list-group'>
  <% @achievements.each do |a| %>
    <div class="achievement">
    <li class='list-group-item active'><strong><%= a.name %></strong></li>
    <li class='list-group-item'><%= a.headline %></li>
    <li class='list-group-item'><%= a.description %></li>
    <li class='list-group-item'><%= a.date %></li>
    </div>
    <br>
  <% end %>

<%= link_to 'Add Achievement', new_achievement_path %>
